{
  "name": "prelude-api-testing",
  "version": "1.0.0",
  "description": "Modern API smoke tests for preludeelectronics.com using Node.js and Express",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "allure:generate": "node scripts/preserve-allure-history.js && allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "allure:run": "npm test && npm run allure:generate && npm run allure:open"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.1",
    "undici": "^6.19.8"
  },
  "devDependencies": {
    "allure-commandline": "^2.38.1",
    "allure-vitest": "^3.7.1",
    "supertest": "^7.0.0",
    "vitest": "^4.1.5"
  }
}
