{
  "name": "cypresstesting",
  "version": "1.0.0",
  "description": "Cypress E2E tests for preludeelectronics.com using POM and Allure reporting",
  "private": true,
  "type": "module",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "cypress run --browser electron",
    "test:chrome": "cypress run --browser chrome",
    "test:smoke": "cypress run --spec 'cypress/e2e/smoke.cy.js' --browser electron",
    "allure:generate": "node scripts/preserve-allure-history.cjs && allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "allure:run": "npm run test && npm run allure:generate && npm run allure:open"
  },
  "keywords": [
    "cypress",
    "e2e",
    "pom",
    "allure",
    "prelude"
  ],
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.2",
    "allure-commandline": "^2.38.1",
    "cypress": "^14.5.4",
    "dotenv": "^16.6.1"
  }
}